[package]
name = "candelora-metal-kernels"
version = "0.1.0"
edition = "2021"

[dependencies]
candle-core = { version = "0.9.2" }
half = "2.4"
candle-metal-kernels = { version = "0.9.2", optional = true }
objc2-metal = { version = "0.3.2", optional = true }

[features]
default = []
metal = ["candle-core/metal", "candle-metal-kernels", "objc2-metal"]
